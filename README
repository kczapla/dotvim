Installation:
	
	git clone git://github.com/perunn/dotvim.git ~/.vim

Create symlinks:

	ln -s ~/.vim/vimrc ~/.vimrc
	ln -s ~/.vim/gvimrc ~/.gvimrc

Submodules if u are using pathogen

	git submodule init #Initializing sub modules
	git submodule update

SYMBOLS

mkdir ~/.fonts
cp ~/.vim/symbols/PowerlineSymbols.otf ~/.fonts/
fc-cache -vf ~/.fonts/

mkdir -p ~/.config/fontconfig/conf.d/
cp ~/.vim/symbols/10-powerline-symbols.conf ~/.config/fontconfig/conf.d/


For vundle

git clone https://github.com/gmarik/vundle.git ~/.vim/bundle/vundle
touch .gitignore
    echo "bundle/*" >> .gitignore
    echo "~/.vim/bundle/* >> .gitignore
    git status
    git add .gitignore
    git commit -am "Add .gitignore"

Or use automatic way executing install.sh
It will fix problem with symbols and install vundle plugin

Compile vim from source
	
	cd ~
	hg clone https://code.google.com/p/vim/
	cd vim
	./configure --with-features=huge \
         	   --enable-multibyte \
	            --enable-rubyinterp \
        	    --enable-pythoninterp \
	            --with-python-config-dir=/usr/lib/python2.7/config \
	            --enable-perlinterp \
	            --enable-luainterp \
        	    --enable-gui=gtk2 --enable-cscope --prefix=/usr
	make VIMRUNTIMEDIR=/usr/share/vim/vim74
	sudo make install
	vim --version  
